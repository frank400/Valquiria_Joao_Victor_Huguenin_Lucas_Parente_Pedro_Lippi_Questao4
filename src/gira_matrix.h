#ifndef QUESTION4_GIRA_MATRIX_H
#define QUESTION4_GIRA_MATRIX_H

#include <stdio.h>
#include <string.h>

#define size 23

void gira_sentido_horario(char grade[size][size]);

void gira_sentido_anti_horario(char grade[size][size]);

void espelha_vertical(char grade[size][size]);

void espelha_horizontal(char grade[size][size]);

#endif //QUESTION4_GIRA_MATRIX_H
